{% extends 'base.html.twig' %}

{% block title %}Edition{% endblock %}
{% form_theme form 'partiels/_collection.html.twig' %}

{% block body %}
    <div class="container">
    <h1 class="text-center mt-lg-3">Edition du voyage en {{ voyage.lieu }}</h1>
    {{ include('voyage/_form.html.twig', {'button_label': 'Update'}) }}
    <a href="{{ path('voyage_index') }}" class="btn btn-outline-secondary">Retour à la liste</a>
    {{ include('voyage/_delete_form.html.twig') }}
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/imageVoyage.js') }}"></script>
{% endblock %}
